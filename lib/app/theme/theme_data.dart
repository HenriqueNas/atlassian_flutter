import 'package:flutter/foundation.dart';

/// {@template theme_data}
/// Based on [ThemeData] from Flutter.
/// Implements the atlassian design system theme.
/// To know more about it, check out the [design system documentation](https://atlassian.design/).
/// {@endtemplate}
class ThemeData with Diagnosticable {
  /// Creates a theme with fallback values.
  const ThemeData.fallback();
}
